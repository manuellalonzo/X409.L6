name: Build Our Container 
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  BuildMyAppContainerImage:
    runs-on: ubuntu-latest
    steps:
	uses: actions/checkout@v4
      - name: Build Image
        run: docker iamge build .
      
